---
phase: 01-core-platform-agentic-foundation
plan: 09
type: execute
wave: 5
depends_on: ["01-07", "01-08"]
files_modified: []
autonomous: false
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07, AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05, AGENT-06]

must_haves:
  truths:
    - "docker compose up shows all 4 services healthy"
    - "hello_world plugin appears in plugin_registry table"
    - "hello.world.v1 event delivered in startup logs"
    - "Login as chef_user returns JWT cookie and redirects to /chef"
    - "Chef cannot access /gm (403)"
    - "Agent run writes outcome to wiki/hello_world/outcomes.md"
    - "Two chef users have separate wiki/users profiles"
  artifacts:
    - path: "wiki/hello_world/outcomes.md"
      provides: "Written by agent during integration test"
      contains: "## ["
    - path: "wiki/users/chef_user.md"
      provides: "Profile created by agent for first chef user"
    - path: "wiki/users/chef_user2.md"
      provides: "Separate profile for second chef user — proves AGENT-06"
  key_links:
    - from: "all 5 Phase 1 success criteria (ROADMAP.md)"
      to: "observable system behavior"
      via: "human verification of docker compose + browser login + agent run"
      pattern: "success_criteria"
---

<objective>
Integration checkpoint: verify all 5 Phase 1 success criteria from ROADMAP.md are met. This plan has no code tasks — it is a human-verify gate confirming the full system works end-to-end before Phase 1 is declared complete.

Purpose: Gate all 13 Phase 1 requirements through observable verification. Executor runs the automated test suite and integration scripts; human confirms the interactive flows (Docker health, browser login, agent round-trip).
Output: Confirmation that Phase 1 is production-ready to hand off to Phase 2.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-platform-agentic-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite + seed DB</name>
  <files></files>
  <read_first>
    - .planning/phases/01-core-platform-agentic-foundation/01-VALIDATION.md (per-task verification map — all test commands)
    - tests/conftest.py (verify AGENT_PROVIDER=mock is set before app imports)
  </read_first>
  <action>
Run all automated tests and integration checks in sequence:

Step 1 — Install backend dependencies (if not already installed):
  pip install -r requirements.txt

Step 2 — Run full pytest suite:
  cd C:/Users/mflma/workspace/tavernia
  pytest tests/ -v --tb=short

Expected: All tests PASS (they should be green by now — if any still FAIL, identify which plan needs a gap closure fix before proceeding).

Step 3 — Boot Docker services:
  cp .env.example .env
  docker compose up -d
  Sleep 30 seconds for healthcheck intervals to pass.
  docker compose ps

Step 4 — Run DB migration and seed:
  docker compose exec api alembic upgrade head
  docker compose exec api python scripts/seed_db.py

Step 5 — Verify plugin registry (CORE-02):
  docker compose exec api python -c "
import asyncio
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession
from sqlalchemy import text
import os
async def check():
    engine = create_async_engine(os.environ['DATABASE_URL'])
    async with async_sessionmaker(engine, class_=AsyncSession)() as db:
        result = await db.execute(text('SELECT name, version FROM plugin_registry'))
        rows = result.fetchall()
        print('plugin_registry rows:', rows)
        assert any(r[0] == 'hello_world' for r in rows), 'FAIL: hello_world not in plugin_registry'
        print('CORE-02 PASS: hello_world registered')
    await engine.dispose()
asyncio.run(check())
"

Step 6 — Verify health endpoint (CORE-01):
  curl http://localhost:8000/health
  Expected: {"status":"ok"}

Step 7 — Verify agent round-trip (AGENT-01 through AGENT-06):
  docker compose exec api python -c "
import asyncio, os
os.environ['AGENT_PROVIDER'] = 'mock'
from app.agents.loader import get_agent_provider
agent = get_agent_provider()
async def run():
    result = await agent.run('Test hello world domain', 'hello_world', username='chef_user')
    print('AgentResult:', result)
    result2 = await agent.run('Second chef test', 'hello_world', username='chef_user2')
    print('AgentResult2:', result2)
    import os; assert os.path.exists('wiki/users/chef_user.md'), 'FAIL: chef_user profile missing'
    assert os.path.exists('wiki/users/chef_user2.md'), 'FAIL: chef_user2 profile missing'
    assert os.path.exists('wiki/hello_world/outcomes.md'), 'FAIL: outcomes.md missing'
    print('AGENT-04 PASS: outcome written')
    print('AGENT-05/06 PASS: distinct user profiles created')
asyncio.run(run())
"

Step 8 — Front-end build:
  cd C:/Users/mflma/workspace/tavernia/frontend && npm run build
  Expected: Build exits 0 with no TypeScript errors.
  </action>
  <verify>
    <automated>cd C:/Users/mflma/workspace/tavernia && pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>pytest suite all green; docker compose ps shows 4 services healthy; plugin_registry has hello_world row; wiki/hello_world/outcomes.md exists; wiki/users/chef_user.md and wiki/users/chef_user2.md are separate files; frontend npm build exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 system:
- Docker: 4 services (postgres, redis, api, celery-worker) all healthy
- Plugin system: hello_world plugin registered, hello.world.v1 event delivered
- Auth/RBAC: JWT httpOnly cookie, role-gated routes
- Agentic framework: AgentProvider loads context + wiki, writes outcomes, creates user profiles
- Frontend: Login page, ProtectedRoute, three role shell pages
  </what-built>
  <how-to-verify>
Phase 1 success criteria from ROADMAP.md — verify each one:

1. CORE-01 (Docker health):
   Run: docker compose up -d && sleep 30 && docker compose ps
   Expected: all 4 services show "(healthy)" status

2. CORE-02/03 (Plugin + event bus):
   Run: docker compose exec api python -c "from sqlalchemy import ...; [check plugin_registry]"
   Expected: hello_world row in plugin_registry (done in Task 1)
   Check docker compose logs api | grep "hello.world.v1"
   Expected: delivery log line from HelloWorldPlugin.on_event

3. CORE-05/06 (Auth + RBAC):
   Open http://localhost:5173 in a browser
   Expected: redirects to /login
   Login with: username=chef_user, password=chef_pass123
   Expected: redirects to /chef; shows "Chef Dashboard"
   Navigate to http://localhost:5173/gm manually
   Expected: redirects to /unauthorized (wrong role)
   Open DevTools > Application > Cookies > localhost
   Expected: access_token cookie with HttpOnly flag checked

4. AGENT-01 through AGENT-06 (Agentic framework):
   Run: cat wiki/hello_world/outcomes.md
   Expected: file exists with YAML frontmatter and at least one ## [timestamp] entry
   Run: cat wiki/users/chef_user.md && cat wiki/users/chef_user2.md
   Expected: TWO separate files (proves AGENT-06 — different profiles despite same role)

5. CORE-07 (Shared schema):
   Run: docker compose exec api python -c "from app.core.schema import Base; print(list(Base.metadata.tables.keys()))"
   Expected: 7 tables listed

Type "approved" if all 5 criteria pass. Otherwise describe which criterion failed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed and which plan needs a fix</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 1 success criteria from ROADMAP.md:
1. docker compose ps — all 4 services healthy
2. plugin_registry has hello_world; startup logs show hello.world.v1 delivery
3. Login as chef_user → /chef; attempt /gm → /unauthorized; cookie shows HttpOnly in DevTools
4. wiki/hello_world/outcomes.md exists with timestamp entries; wiki/users/ has chef_user.md and chef_user2.md as SEPARATE files
5. Base.metadata.tables contains 7 tables

Full test suite:
  pytest tests/ -v --tb=short
  Expected: 0 failures, 0 errors
</verification>

<success_criteria>
- pytest tests/ shows all green (0 failures)
- docker compose ps: all 4 services healthy
- GET /health returns {"status": "ok"}
- plugin_registry table has hello_world row after startup
- Startup logs contain "received hello.world.v1" from HelloWorldPlugin
- Login as chef_user returns 200, sets httpOnly cookie, browser redirects to /chef
- Navigating to /gm as chef_user returns redirect to /unauthorized
- wiki/hello_world/outcomes.md exists with at least one outcome entry
- wiki/users/chef_user.md and wiki/users/chef_user2.md are SEPARATE files (AGENT-06)
- Base.metadata.tables has exactly 7 tables: users, roles, user_roles, vendors, gl_accounts, audit_log, plugin_registry
- frontend npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-platform-agentic-foundation/01-09-SUMMARY.md`

Update ROADMAP.md Phase 1 status: mark as complete.
Update STATE.md: status = complete, current_focus = Phase 2.
</output>
